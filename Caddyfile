{
    http_port {$CADDY_PORT:80}
}

http:// {
    root * {$FILE_ROOT:/srv}

    basicauth * {
        {$SMB_USER:samba} {$SMB_PASS_HASH}
    }

    file_server {
        browse
        hide .DS_Store ._.DS_Store ._*
    }
}